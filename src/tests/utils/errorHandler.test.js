import errorHandler, { errorCodes } from "../../utils/errorHandler";

test("", () => {});

// test("Correct message is returned when email is already in use", () => {
//     const error = {
//         code: errorCodes.EMAIL_ALREADY_USED,
//         email: "test@test.te"
//     };

//     const result = errorHandler(error);

//     expect(result).toBe("This provider's email is already used by your Googles account.");
// });